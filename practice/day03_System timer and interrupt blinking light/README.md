# 03

## 功能
使用串口中断接收数据，并将接收到的数据通过串口回显出来。

## 使用内容
- STM32 USART
- 串口中断
- NVIC中断配置

## 实现思路
1. 先初始化串口
2. 开启串口接收中断
3. 在中断回调中读取接收到的数据
4. 再通过串口发送出去，实现回显

## 运行现象
串口助手发送一个字符，开发板接收到后会原样返回。

## 遇到的问题
- 一开始没有开启中断，导致收不到数据
- 没有重新开启接收，导致只能接收一次

## 后续优化
后面准备加入字符串接收和命令解析
